{% extends "layout.njk" %}
{% block content %}

  <main class="recipe-page main">
    <img class="image" src="{{recipe.image}}" alt="Foto de {{recipe.title}}">
    <h1 class="title">{{recipe.title}}</h1>
    <p class="author">{{recipe.author}}</p>

    <section class="-active">
      <div class="container">
        <h2 class="preparation">Ingredientes</h2>
        <button class="button">Esconder</button>
      </div>
      <ul class="text -list">
        {% for ingredient in recipe.ingredients %}
          <li>{{ ingredient }}</li>
        {% endfor %}
      </ul>
    </section>
    <section class="-active">
      <div class="container">
        <h2 class="preparation">Modo de Preparo</h2>
        <button class="button">Esconder</button>
      </div>
      <ol class="text -list">
        {% for step in recipe.preparation %}
          <li>{{ step }}</li>
        {% endfor %}
      </ol>
    </section>
    <section class="-last -active">
      <div class="container">
        <h2 class="preparation">Informações Adicionais</h2>
        <button class="button">Esconder</button>
      </div>
      <p class="text">{{ recipe.information }}</p>
    </section>
  </main>

{% endblock %}
